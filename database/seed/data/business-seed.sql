-- ============================================================
-- nexus_data business seed — INTENTIONALLY EMPTY (ARCH-02)
--
-- 2026-05-04: All 14 mock business tables (lot_status, sales_order,
-- cp_ft_result, wip_inventory, reliability_report, rma_record,
-- price_book, npi_gate_checklist, yield_events, lot_status_history,
-- lot_daily_flow, v_lot_status_pe, v_lot_status_sales,
-- yield_daily_trend) were removed by:
--   - database/migrations/data/V008__drop_demo_business_tables.sql
--   - database/migrations/V090__drop_mock_resource_bindings.sql
--
-- Real business data now flows from `ds:pg_k8` (Phison Greenplum,
-- schema `tiptop`) and CDC sinks landed by ETL_SVC into nexus_data.
-- See `database/seed/dev-seed.sql` (`ds:pg_k8` bootstrap section)
-- and `docs/standards/path-c-rls-demo-pg_k8-tiptop.md`.
--
-- This file is kept (rather than deleted) because deploy/docker-compose/
-- init-db.sh sources every *.sql under database/seed/data/. Removing
-- it would require a Makefile / init-db.sh change. An empty SQL file
-- is the lowest-risk path.
-- ============================================================
